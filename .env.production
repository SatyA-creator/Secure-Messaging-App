# ==================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# ==================================================================
# These variables should be set in your deployment platforms
# ==================================================================

# ===== VERCEL (Frontend) Environment Variables =====
# Set these in Vercel Dashboard → Settings → Environment Variables

VITE_API_URL=https://secure-messaging-app-backend.onrender.com/api/v1
VITE_WS_URL=wss://secure-messaging-app-backend.onrender.com


# ===== RAILWAY (Backend) Environment Variables =====
# Set these in Railway Dashboard → Variables

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
DEBUG=false
ENVIRONMENT=production

# Database (Supabase PostgreSQL)
# CRITICAL: Use port 5432 (direct) NOT 6543 (pooler) for Render compatibility
# Pooler port 6543 causes timeout errors on Render
DATABASE_URL=postgresql://postgres.ycnilziiknmahekkpveg:Secure%40123@aws-1-ap-south-1.pooler.supabase.com:5432/postgres
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10

# JWT Security
JWT_SECRET_KEY=your-super-secret-256-bit-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=30

# CORS Origins
# NOTE: Railway doesn't support array syntax, you'll need to set this in code
# The config.py already includes the Vercel URL in CORS_ORIGINS list

# Email Configuration (Resend)
RESEND_API_KEY=re_123456789_your_actual_resend_api_key
VERIFIED_DOMAIN=quantchat.live

# Frontend & WebSocket URLs
FRONTEND_URL=https://secure-messaging-app-omega.vercel.app
WEBSOCKET_URL=wss://secure-messaging-app-backend.onrender.com

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Security
BCRYPT_SALT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=15

# Redis (if you add Redis to Railway)
# REDIS_URL=redis://default:password@redis.railway.internal:6379
